{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<h1>{{admin.userList.title}}</h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col">id</th>
      <th scope="col">{{admin.userList.colLName}}/th>
      <th scope="col">{{admin.userList.colFName}}</th>
      <th scope="col">{{admin.userList.colRoles}}</th>
    </tr>
  </thead>
  <tbody>
    {% for user in users %}
        <tr>
        <th>{{user.id}}</th>
        <th scope="row">{{user.nom}}</th>
        <td>{{user.prenom}}</td>
        <td>{{user.roles|join(', ')}}</td>
        </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}